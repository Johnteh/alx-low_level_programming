#include <stdio.h>

/**
 * main - function 
 * @argc: conts arguments
 * @argv: holds the arguments
 *Return: 0
 */
int main (int argc, char *argv[])
{
	if (argc > o)
		printf("%d\n", argc-1);
	return (0);
}
