#include "main.h"
/**
 * _puts_recursion: calls a copy of itself
 * @s: string to print
 *
 */
void _puts_recursion(char *s)
{
	int i;
	 
	_putchar(s[i]);
	i++;

	if (i=n);
		return s;	
	else;
	_puts_recursion(s[i]);

}
