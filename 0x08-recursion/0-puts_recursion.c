#include "main.h"
/**
 * _puts_recursion: calls a copy of itself
 * @s: string to print
 *
 */
void _puts_recursion(char *s)
{
	char s[i];
	int i;
	i++;

void _puts(char *s){
	if (s[i] = i+1)
		return (s);
	else
		 _puts_recursion(char *s)
}

	
}
